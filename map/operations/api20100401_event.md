<!-- Generated file — do not edit; regenerated with the SDK. -->

# Api20100401Event — operations

Accessor: `client.api20100401_event` · Source: `twilio/apis/api20100401_event.py` · 1 operation

Each `###` block is one operation and assumes `sdk-map.md` is loaded: blocks omit what its invariants table covers and are otherwise self-contained, so chunk at block level. Signatures are the sync parsed spelling; the async and raw spellings take the same parameters (see sdk-map.md). **Type sources** names the module declaring each type an operation mentions, so resolving a body, return or error payload is a lookup rather than a search; the runtime types `RawError` and `ApiResult` are excluded.

### client.api20100401_event.list_call_event

- **Route**: `GET /2010-04-01/Accounts/{AccountSid}/Calls/{CallSid}/Events.json`
- **Server**: `default`
- **Signature**: `def list_call_event(account_sid: str, call_sid: str, *, page_size: int | None = None, page: int | None = None, page_token: str | None = None, request_options: RequestOptionsOrDict | None = None)`
  - required, positional: `account_sid`, `call_sid`
- **Params**: `account_sid` — path `AccountSid` · `call_sid` — path `CallSid` · `page_size` — query `PageSize` · `page` — query `Page` · `page_token` — query `PageToken`
- **Returns (parsed)**: `ListCallEventResponse`
- **Returns (raw)**: `ApiResult[ListCallEventResponse, RawError]`
- **Error**: `RawError` — **Case B**

| Type | Source |
| --- | --- |
| `ListCallEventResponse` | `twilio/models/list_call_event_response.py` |

